import React,{useState} from "react";
import { bookings } from "../../data";
export default function Bookings(){const [rows,setRows]=useState(bookings);return <Page title="Bookings" sub="Accept, reject and manage reservations for your hotel."><div className="filter-bar"><input placeholder="Search guest or booking..."/><select><option>All Status</option><option>Confirmed</option><option>Pending</option></select></div><div className="panel table-panel"><Table rows={rows} setRows={setRows}/></div></Page>}
function Table({rows,setRows}){return <div className="table-wrap"><table><thead><tr><th>GUEST</th><th>ROOM</th><th>DATES</th><th>AMOUNT</th><th>STATUS</th><th>ACTIONS</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td><b>{r.guest}</b><small>{r.email}</small></td><td>{r.room}</td><td>{r.dates}</td><td>{r.amount}</td><td><span className={`status ${r.status.toLowerCase()}`}>{r.status}</span></td><td>{r.status==="Pending"&&<><button className="mini success" onClick={()=>setRows(rows.map(x=>x.id===r.id?{...x,status:"Confirmed"}:x))}>Accept</button><button className="mini danger">Reject</button></>}</td></tr>)}</tbody></table></div>}
function Page({title,sub,children}){return <div className="dash-content"><div className="page-title"><h1>{title}</h1><p>{sub}</p></div>{children}</div>}