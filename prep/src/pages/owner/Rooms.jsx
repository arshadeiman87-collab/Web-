import React,{useState} from "react";
import { Plus, Edit3, BedDouble } from "lucide-react";
const initial=[["Deluxe Suite","12","$450","Available"],["Standard King","28","$280","Available"],["Premium Suite","8","$650","Occupied"]];
export default function Rooms(){const [rooms,setRooms]=useState(initial); const [show,setShow]=useState(false); return <Page title="Rooms" sub="Manage room categories, pricing, amenities and availability."><div className="toolbar"><button className="gold-btn" onClick={()=>setShow(true)}><Plus size={18}/> Add Room Category</button></div><div className="room-grid">{rooms.map((r,i)=><div className="room-card" key={i}><div className="room-icon"><BedDouble/></div><h3>{r[0]}</h3><p>{r[1]} rooms</p><strong>{r[2]} <small>/ night</small></strong><span className={`status ${r[3].toLowerCase()}`}>{r[3]}</span><button className="outline-btn"><Edit3 size={16}/> Edit</button></div>)}</div>{show&&<div className="modal-backdrop"><div className="modal"><h2>Add Room Category</h2><input placeholder="Room category name"/><input placeholder="Number of rooms" type="number"/><input placeholder="Price per night" type="number"/><textarea placeholder="Amenities"/><div className="modal-actions"><button className="outline-btn" onClick={()=>setShow(false)}>Cancel</button><button className="gold-btn" onClick={()=>{setShow(false);setRooms([...rooms,["New Room","5","$300","Available"]])}}>Add Room</button></div></div></div>}</Page>}
function Page({title,sub,children}){return <div className="dash-content"><div className="page-title"><h1>{title}</h1><p>{sub}</p></div>{children}</div>}