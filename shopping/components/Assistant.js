 "use client";
import {useState} from "react";
import {products} from "../lib/catalog";
export default function Assistant(){const [q,setQ]=useState("");const [messages,setMessages]=useState([{role:"ai",text:"Hi! I can recommend products, compare items, or explain store policies. What are you shopping for?"}]);function send(){if(!q.trim())return;const text=q;setMessages(m=>[...m,{role:"user",text},{role:"ai",text:answer(text)}]);setQ("")}function answer(text){const t=text.toLowerCase();if(t.includes("return")||t.includes("refund"))return "Our return policy supports eligible returns within 30 days of delivery. Source: Store Returns Policy. If your situation is unusual, I can hand it to a support specialist.";if(t.includes("compare"))return `For a catalog comparison, ${products[0].name} emphasizes portability while ${products[1].name} focuses on battery life. I can compare price, display, battery and weight side-by-side.`;if(t.includes("laptop"))return `Based on the catalog, ${products[0].name} is a strong travel-oriented option at $${products[0].price}. Source: Catalog product attributes.`;return "I can help with product recommendations, comparisons, and policy questions. For requests outside approved catalog or policy data, I’ll route you to support rather than guess."}return <div className="chat"><div className="messages">{messages.map((m,i)=><div key={i} className={`msg ${m.role}`}>{m.text}</div>)}</div><div className="chat-input"><input value={q} onChange={e=>setQ(e.target.value)} onKeyDown={e=>e.key==="Enter"&&send()} placeholder="Ask about products or policies..." /><button onClick={send} className="button primary">Ask</button></div></div>}
